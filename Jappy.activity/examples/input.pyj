
print ("Hola, como te llamas?")
async_input(recibe)

def recibe(nombre):
    if nombre:
        window.nombre = nombre
        print(nombre + "<br>")
        print("Cual es la contraseña?")
        async_input(verifica)
    else:
        return False
        
def verifica(seña):
    if seña=='lala':
        cls()
        print ("Si. Bienvenid@ " + nombre +"!")
        return True
    print("No. <br>Vamos, intenta leer la <i>seña</i> en el código.")
    return False
